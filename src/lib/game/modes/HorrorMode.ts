import GameMode from './GameMode';

export default class HorrorMode extends GameMode {
  setupEnvironment(): void {
    // TODO: dark atmosphere, limited visibility
  }
  updateEnvironment(time: number): void {
    void time;
  }
}
