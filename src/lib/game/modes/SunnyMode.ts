import GameMode from './GameMode';

export default class SunnyMode extends GameMode {
  setupEnvironment(): void {
    // TODO: bright lighting, clear sky
  }
  updateEnvironment(time: number): void {
    void time;
  }
}
